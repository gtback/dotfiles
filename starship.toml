format="""
$all\
$fill\
$kubernetes\
$gcloud\
$aws\
$line_break\
$jobs\
$battery\
$status\
$character"""

[kubernetes]
style = "blue bold"
disabled = false

[kubernetes.context_aliases]
"gke_.*_(?P<cluster>[\\w-]+)" = "gke-$cluster"

[time]
disabled = false
