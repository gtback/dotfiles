#!/bin/bash

# Detect formuale, casks, taps, etc. that are in more than one Brewfile

set -euo pipefail

fd Brewfile "${HOME}" | grep -v "${BREWFILE_IGNORE:-xxxxxx}" | xargs cat | grep -E -v "(^#|^$)" | sort | uniq -c | grep -v "^   1 "
